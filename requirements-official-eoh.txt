# Optional: only needed when running official EOH experiments (not for unit tests)
# These are dependencies of the vendored Agent_EOH/ framework, not of the eoh_rag package itself.
requests
torch
numba
python-docx
