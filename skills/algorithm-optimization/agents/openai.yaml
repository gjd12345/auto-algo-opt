interface:
  display_name: "Algorithm Optimization"
  short_description: "Drive recoverable heuristic evolution with EoH"
  default_prompt: "Use $algorithm-optimization to run a bounded, evidence-backed algorithm evolution session."

policy:
  allow_implicit_invocation: true
