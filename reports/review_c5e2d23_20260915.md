# c5e2d23 分析复核与工程收尾

核验基线：`c5e2d23f26139df2349c13cf12f72598bf5a0680`。本次未调用真实 provider、未运行正式效果实验。

## 对原分析的核验

| 判断 | 本次核验及边界 |
| --- | --- |
| 基线相对 3b69f29 前进八个提交 | 本地历史吻合。远程四个 CI job 的历史结论本次未重新查询，不等同于本次修改后 CI 已通过。 |
| Memory 有版本、完整性和作用域；没有 utility 学习 | 当前实现吻合：先选最高版本再校验，search 为词法命中和本项目/shared 权重。未开展新的 Memory 全量审计。 |
| KB 与生产 wheel 隔离 | pyproject 显式包清单不含 knowledge_tools；维持隔离。 |
| KB 未进入 CI | 确认。新增 Python 3.11、Ubuntu/Windows 独立 knowledge job。 |
| current.path fallback | 原来已有模拟 symlink 失败的测试，本次补上通过 store 与 current 两个入口解析后的断言。 |
| KB 实验身份 | 确认 knowledge_context 仅为宿主记录。主 pilot 的 extra 现在显式 hash knowledge_mode=off，拒绝 frozen_context 或混入其他 knowledge_* 元数据；这不是外部 Agent 阅读行为的技术隔离。 |
| 真实探针只有 gate 验证 | 确认第一轮 8 次请求预留，第二轮 seed 不足终止。纠正原报告：7 次完成含 1 次 probe，生成完成实际为 6 次（4 stop、2 length），另 1 次 unknown。 |
| 新 mini 非退化 | First Fit 与 Best Fit 可区分；但 Best Fit 已全部达到训练最优，不能据此设计改善率主实验。 |
| 多精英两轮未验证 | 对历史真实跑次成立。本次已完成官方 EoH 的两轮 localhost fixture，并保留完整精简证据；真实成功热启动仍待验证。 |
| A/B/C/D 未完成 | 仍成立；本次只完成一条多精英 fixture 闭环。 |

## 已修复

1. CI 增加 Knowledge 双平台测试及固定 release 校验。冻结 release 与 evidence 使用 `-text` 保留实际字节，防止 Windows 换行转换。逐个核对 Git HEAD 中 release 资产与 manifest，0 个 hash 不匹配。
2. 修正闭环脚本：使用 obp_evolution_mini；fixture 交替返回 First Fit/Best Fit 并由实际 evaluator 打分；要求两个不同 fitness、第二轮 seed 重评及生成证据。最后种群和 incumbent 均取第二轮，不再固定第一轮。CI 的双平台 EoH job 执行此闭环并上传产物。
3. 新增 `tools/export_benchmark_evidence.py`：只导出终态 Session；核对源文件与数据库 hash；保留训练事实、计划、上下文、种群、种子选择、archive 和请求/评测账本。可附三类 heldout report inputs，并重建报告确认一致。导出附 SHA256SUMS.json；拒绝已有目标、hash 漂移、额外文件及明显密钥字符串。
4. 主 pilot 冻结 KB off，保留 C/D 只有 agent_guidance 不同的合同。KB frozen_context 未实施，不允许宣称 KB 因果实验已具备准入条件。
5. 额外发现并修复工作区 Plan 示例：原 `print(Path(...) / 'current').resolve()` 会对 print 的 None 返回值调用 resolve，也未执行 current.path fallback。改用 `resolve_release()`；已发布 immutable release 不就地修改，该示例修正需下一版发布纳入。

## 验证证据

- Knowledge + benchmark 定向测试：66 passed。
- 新增精简包防篡改测试及修改后的 pilot 因子测试：2 passed。
- 固定 KB release 校验：通过，manifest SHA256 为 `bcf8b59581d4e0e3ac0053407e1aa1856e6cf35c879943cabefc61068171c61a`。
- 两轮官方 EoH fixture：通过。第一轮种群 fitness 为 0 与 0.14821428571428572；累计 20 次训练评测 = 2 baseline + 2 seed 重评 + 16 novel，localhost 请求 18 次，外部 provider 请求 0。incumbent 始终为 0。
- 三类 selection 的 heldout 报告均由持久化输入重建；脚本退出码 0。
- 修改的 Python 可 compileall；git diff --check 通过。
- 本机验证为 Windows Python 3.11；Ubuntu 与远程 CI 等待推送后执行，本次不宣称通过。

证据入口：[历史真实探针](evidence/obp_evolution_mini_real_20260914_01/bundle.json)、[两轮 fixture](evidence/obp_two_round_fixture_20260915/bundle.json)。

```bash
python tools/export_benchmark_evidence.py --verify reports/evidence/obp_evolution_mini_real_20260914_01
python tools/export_benchmark_evidence.py --verify reports/evidence/obp_two_round_fixture_20260915
```

精简包是可核对的审计材料，不是可恢复的 Session：SHA 清单保证内容完整性，不构成第三方签名；provider exchange 引用仍指向被省略的原始会话。真实探针未做 heldout，包内不伪造 selection/test。fixture 包包含三类完整报告输入和输出。训练 solver 预算不包含导出后独立的 heldout 复评成本，效果实验必须另行记录后者。

后续步骤见 [实验规划](../docs/experiment_plan_after_c5e2d23.md)。
