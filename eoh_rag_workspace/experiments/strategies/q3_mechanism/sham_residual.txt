CONTROLLED STRATEGY CONTEXT
The following section always contains exactly two card slots. Use strategy content only when a slot explicitly provides it.

[CARD SLOT 1]
Type: control slot
Purpose: preserve the two-slot format without providing a strategy.
Content: no task-specific scoring rule, threshold, formula, ordering, or implementation hint is present.
Instruction: ignore this slot when designing the heuristic.
[NEUTRAL LENGTH PADDING]
This padding only equalizes context length and adds no task rule. This padding only equalizes context length and adds no task rule. This padding only equalizes context length and adds no task rule. This padding only equalizes context length and adds no task rule. This padding only equalizes context length and adds no task rule. This padding only equalizes context length and adds no task rule. This padding only equalizes context length and adds no task rule. This padding only equalizes context length and adds no task rule. This padding only equalizes context length and adds no task rul
[END CARD SLOT 1]

[CARD SLOT 2]
Type: frozen strategy
Title: OBP FunSearch Residual Polynomial Skill Card
Summary: FunSearch-style residual penalty: combine tight-fit reward with polynomial penalty on unusable gaps.
Constraints:
- Do not use future items.
- Keep polynomial numerically stable.
Strategy:
Skill: obp_residual_poly
When: best-fit leaves many unusable tiny gaps.
Do: compute residual r=rem-item; reward tight fit but add penalty for residual ranges that are likely unusable, e.g. r near small isolated gaps.
Fallback: if formula ties, use best-fit.
Safety: scores must be finite for every feasible bin.
[NEUTRAL LENGTH PADDING]
This padding only equalizes context length and adds no task rule. This padding only equalizes context length and adds no task rule. This padding only equalizes context length and adds no task rule. This padding only equalizes context length and adds no task r
[END CARD SLOT 2]
